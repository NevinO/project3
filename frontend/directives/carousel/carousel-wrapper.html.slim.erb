uib-carousel interval="carouselInterval"
  uib-slide ng-repeat="slide in slides" active="slide.active"
    .wrapper-slider__img
      img.img-responsive ng-src="{{slide.image}}"
    .carousel-caption
      p {{slide.text}}
